<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <MultiSelectListPreference
        android:key="news_selection"
        android:entryValues="@array/pref_news_selection_values"
        android:title="@string/pref_title_news_selection"
        android:entries="@array/pref_news_selection_titles"
        android:defaultValue="@array/empty_array"/>

    <!--
         NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it.
    -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    
    <PreferenceScreen 
        android:key="password_setting" android:title="@string/pref_password_settings">

    </PreferenceScreen>
    
    
    
</PreferenceScreen>
